import React from "react";
import Layout from "../Components/Layout";

function AllProjects_CreateNew() {
  return <Layout activeTab={1} activeSubProjects={0} />;
}

export default AllProjects_CreateNew;
