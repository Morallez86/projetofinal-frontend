import React from "react";

function ResourcesTable({
    data,
    loading,
    pagination,
    paginationServer,
    paginationTotalRows,
    onChangePage,
    onChangeRowsPerPage,
    rowsPerPage,
    setFilterText,
  }) {
  return <div>ResourcesTable</div>;
}

export default ResourcesTable;
